environment: test

prometheus:
  namespace: sybrenbolandit
  slack:
    alertChannel: mijnns-alerts-ota

fluentd:
  enabled: true
  autoscaling:
    minReplicas: 1
    maxReplicas: 2
    targetCPUUtilizationPercentage: 50
