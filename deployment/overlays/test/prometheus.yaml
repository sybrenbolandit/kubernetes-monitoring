apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: prometheus
spec:
  replicas: 1
  alerting:
    alertmanagers:
      - name: alertmanager-jenkins-project
        namespace: jenkins-project
        port: web
